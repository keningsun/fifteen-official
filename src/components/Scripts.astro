<!-- Webflow runtime scripts + Loops newsletter handler -->
<script is:inline src="/assets/webflow/js/jquery-3.5.1.min.dc5e7f18c8.js" type="text/javascript"></script>
<script is:inline src="/assets/webflow/js/webflow.schunk.74f756804daaabee.js" type="text/javascript"></script>
<script is:inline src="/assets/webflow/js/webflow.schunk.de80f719acaf7ae3.js" type="text/javascript"></script>
<script is:inline src="/assets/webflow/media/webflow.21fd8f95.6764d1ba68c55d51.js" type="text/javascript"></script>
<style>
pre code {
  background: #f5f5f5;
  padding: 1rem;
  display: block;
  border-radius: 6px;
  overflow-x: auto;
  font-family: monospace;
}
</style>
<script is:inline type="text/javascript">
    /* FAQ accordion - ensure expand/collapse works (handles click and toggles w--open) */
    $(document).ready(function() {
      $(document).on('click', '.faq3_accordion .faq3_question', function(e) {
        e.preventDefault();
        e.stopPropagation();
        var $accordion = $(this).closest('.faq3_accordion');
        var $answer = $accordion.find('.faq3_answer');
        var isOpen = $answer.hasClass('w--open');
        $('.faq3_accordion .faq3_question').removeClass('w--open');
        $('.faq3_accordion .faq3_answer').removeClass('w--open');
        if (!isOpen) {
          $(this).addClass('w--open');
          $answer.addClass('w--open');
        }
      });
    });

    /* Loops newsletter form handler */
    $('form[action^="https://app.loops.so"]').each(function (i, el) {
        form = $(el);
        form.submit(function (e) {
          e.preventDefault();
          form = $(e.target);
          action = form.attr('action');
          $.ajax({
            url: action,
            method: "POST",
            data: form.serialize(),
            dataType: "json",
            success: function (data) {
              if (data.success) {
                parent = $(form.parent());
                parent.children('form').css('display', 'none');
                parent.children('.w-form-done').css('display', 'block');
              } else {
                parent = $(form.parent());
                parent.find('.w-form-fail').css('display', 'block');
              }
            },
            error: function () {
              parent = $(form.parent());
              parent.find('.w-form-fail').css('display', 'block');
            }
          });
        });
    });
</script>
